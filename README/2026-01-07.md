1.目前直接把react源码构建的支持sourcemap目前似乎不太现实:

    1.我花了一下午让chatGpt和cursor帮我弄了下，弄不好；
    2.我之前买了个付费视频，视频里也是直接使用yarn link 进行调试的；并且这样也可以满足调试；

所以直接使用yarn link， react仓库只构建需要的包；react构建出来的包也是可以使用的；

2.目标: 用自己的 demo 项目，通过 yarn link 把 React 源码包链接过去

1）先在 React 仓库里构建出可被 link 的产物
还是在 React 仓库根目录：

sudo n
选择  16.x版本的node

如果报没java环境  参考`2025-12-31.md`

yarn build

如果只是测试这2个包，直接运行下面代码，速度更快
yarn build react/index,react/jsx-runtime,react/jsx-dev-runtime,react-dom/index,scheduler --type=NODE_DEV

使用--type=NODE  是为了让包可以被其他项目使用，使用UMD导出的好像不能使用；
不加 scheduler 会报错

构建成功后，一般会在类似路径生成产物（不同版本路径不同）：
build/node_modules/react
build/node_modules/react-dom

你可以用下面命令确认有没有 build 出来：
ls build/node_modules | head

2）在 React 仓库里把包 link 出去
在 React 仓库根目录执行（如果不行就按下面“进入具体包目录”的方式）：
cd build/node_modules/react
yarn link
cd ../react-dom
yarn link

3）在你的 demo 项目里接入 link

创建demo项目:
  npx create-react-app a-react-demo
  cd a-react-demo
进入你的 demo 项目：
yarn link react
yarn link react-dom

然后启动你的 demo：
rm -rf node_modules/.cache   (可恶的缓存)
yarn dev
或
yarn start

4）修改源码并重新构建（重要！）
⚠️ 重要：必须修改源码，而不是修改构建产物！

修改源码的正确位置：
- React 核心代码：`packages/react/src/React.js`
- React Hooks：`packages/react/src/ReactHooks.js`
- React DOM：`packages/react-dom/src/ReactDOM.js`
- 其他文件：`packages/react/src/` 或 `packages/react-dom/src/` 目录下

修改后，在 React 仓库根目录重新构建：
yarn build

构建会自动清理旧的 build 目录，所以不需要手动删除。

⚠️ 常见问题：修改了代码但浏览器没看到效果？

1. 确认修改的是源码（packages/react/src/），不是构建产物（build/node_modules/react）
2. 确认重新运行了构建命令
3. 确认 demo 项目使用的是开发模式（NODE_ENV !== 'production'）
   - create-react-app 默认是开发模式，应该没问题
   - 如果使用生产模式，会加载 react.production.min.js，看不到 console.log
4. 清除 demo 项目的缓存：
   - 删除 node_modules/.cache（如果存在）
   - 重启开发服务器
   - 硬刷新浏览器（Cmd+Shift+R 或 Ctrl+Shift+R）

测试代码示例（已添加到 React.js）：
```javascript
// 在 packages/react/src/React.js 文件开头添加
if (typeof console !== 'undefined') {
  console.log('🚀 React 源码已加载！这是从源码构建的版本');
}
```
  其他问题：
  1.目前手动删除是否是生产环境有点太不方便了，研究下什么问题
    这个问题再项目重启之后好了，也是醉了；

## 5. 无 SourceMap 调试方法（推荐）

由于没有 sourcemap，调试需要一些技巧。以下是几种实用的方法：

### 方法1：在源码中添加调试代码（最推荐）

**优点：** 代码清晰，易于维护，可以添加详细的日志

**步骤：**
1. 在源码中添加 `debugger` 或 `console.log`
   ```javascript
   // 例如：packages/react-reconciler/src/ReactFiberWorkLoop.old.js
   function beginWork(current, workInProgress, renderLanes) {
     debugger; // 浏览器会自动停在这里
     console.log('🔍 beginWork:', {
       tag: workInProgress.tag,
       type: workInProgress.type,
       key: workInProgress.key
     });
     // ... 原有代码
   }
   ```

2. 重新构建：
   ```bash
   yarn build react/index,react/jsx-runtime,react/jsx-dev-runtime,react-dom/index,scheduler --type=NODE_DEV
   ```

3. 清除缓存并重启 demo：
   ```bash
   # 在 demo 项目目录
   rm -rf node_modules/.cache
   yarn start
   ```

4. 打开浏览器 DevTools，触发操作，代码会在 `debugger` 处停止

**常用调试位置：**
- `packages/react-reconciler/src/ReactFiberWorkLoop.old.js` - `beginWork`, `completeUnitOfWork`
- `packages/react-reconciler/src/ReactFiberCommitWork.old.js` - `commitRoot`, `commitMutationEffects`
- `packages/react/src/ReactHooks.js` - `useState`, `useEffect` 等 hooks
- `packages/react-dom/src/ReactDOM.js` - `render`, `hydrate`

### 方法2：Chrome DevTools 搜索函数（快速定位）

**优点：** 不需要重新构建，快速找到函数位置

**步骤：**
1. 打开 demo 页面，打开 Chrome DevTools
2. 切换到 **Sources** 面板
3. 按 `Ctrl/Cmd + P` 打开文件搜索
4. 搜索函数名或文件名：
   - `beginWork` - 开始处理一个 fiber 节点
   - `commitRoot` - 提交根节点
   - `performUnitOfWork` - 执行一个工作单元
   - `dispatchSetState` / `dispatchAction` - useState 的 dispatch
   - `scheduleUpdateOnFiber` - 调度更新
   - `react-dom.development.js` - 直接搜索文件

5. 在函数入口处打断点
6. 触发操作（点击按钮、setState 等）
7. 观察调用栈（Call Stack）了解执行流程

**技巧：**
- 使用 `Ctrl/Cmd + Shift + F` 在所有文件中搜索关键词
- 在 Call Stack 中点击不同层级，查看不同函数的上下文
- 使用 `Watch` 面板监控变量值

### 方法3：使用 console.log 追踪执行流程

**优点：** 不需要打断点，可以看到完整的执行流程

**在源码中添加日志：**
```javascript
// packages/react-reconciler/src/ReactFiberWorkLoop.old.js
function workLoopSync() {
  console.log('🔄 workLoopSync 开始');
  while (workInProgress !== null) {
    console.log('📦 处理 fiber:', workInProgress.type);
    performUnitOfWork(workInProgress);
  }
  console.log('✅ workLoopSync 结束');
}
```

**使用 Chrome DevTools 的 Console 过滤：**
- 使用 `console.log` 的样式：`console.log('%c 文本', 'color: red; font-size: 20px')`
- 使用 `console.group` 分组：
  ```javascript
  console.group('🔍 beginWork');
  console.log('current:', current);
  console.log('workInProgress:', workInProgress);
  console.groupEnd();
  ```

### 方法4：使用 React DevTools Profiler

**优点：** 可视化组件渲染性能，不需要修改代码

**步骤：**
1. 安装 React DevTools 浏览器扩展
2. 打开 DevTools，切换到 **Profiler** 标签
3. 点击录制按钮
4. 在页面上执行操作
5. 停止录制，查看火焰图
6. 点击某个组件，查看渲染时间和原因

### 方法5：在构建文件中直接调试（临时方案）

**⚠️ 注意：** 这种方法不推荐，因为重新构建会丢失修改

**步骤：**
1. 找到构建后的文件：
   - `build/node_modules/react-dom/cjs/react-dom.development.js`
   - `build/node_modules/react/cjs/react.development.js`

2. 直接搜索函数名（使用编辑器或 grep）
   ```bash
   grep -n "function beginWork" build/node_modules/react-dom/cjs/react-dom.development.js
   ```

3. 在函数中添加 `debugger` 或 `console.log`
4. 刷新浏览器即可生效（无需重启服务器）

### 方法6：使用 VSCode 调试（需要配置）

**配置 `.vscode/launch.json`：**
```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Debug React Demo",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/../reactSource/demo1/a-react-demo",
      "sourceMaps": false,
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}
```

**限制：** 由于没有 sourcemap，VSCode 无法直接映射到源码，但可以在构建后的文件中打断点。

### 调试技巧总结

1. **快速定位函数：** 使用 `Ctrl/Cmd + P` 搜索函数名
2. **查看调用栈：** 在 Call Stack 中点击不同层级
3. **监控变量：** 使用 Watch 面板或 Console 输入变量名
4. **条件断点：** 右键断点，设置条件（如 `workInProgress.tag === 1`）
5. **日志断点：** 右键断点，选择 "Add logpoint"，不暂停执行但输出日志
6. **性能分析：** 使用 Performance 面板录制，查看函数执行时间

### 常用调试函数位置参考

| 功能 | 函数名 | 文件位置 |
| --- | --- | --- |
| 开始处理节点 | `beginWork` | `ReactFiberWorkLoop.old.js` |
| 完成节点 | `completeUnitOfWork` | `ReactFiberWorkLoop.old.js` |
| 提交根节点 | `commitRoot` | `ReactFiberCommitWork.old.js` |
| useState | `dispatchSetState` | `ReactFiberHooks.old.js` |
| useEffect | `commitHookEffectList` | `ReactFiberHooks.old.js` |
| 调度更新 | `scheduleUpdateOnFiber` | `ReactFiberWorkLoop.old.js` |
| 渲染 | `render` | `ReactDOM.js` |
