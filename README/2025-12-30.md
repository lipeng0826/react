# 库的安装和调试

## 在仓库内跑官方 demo（fixtures）：最快验证“能进源码”

### 安装 & 基础检查
  
  ```sh
  cd 仓库根目录
  node -v
  yarn -v
  ```

  安装依赖:

  安装依赖之前先把node版本切换到16.x，上次按了半天提示20.x不支持，浪费时间

```sh
  npm i -g npm
  sudo n 16
  n 16
  node -v
```

```sh
yarn
```
  备注：第一次可能安装失败，失败了再次执行yarn安装依赖
       估计得10分钟左右，还挺费时间

### 构建（不带 sourcemap 的开发构建）

  背景：因为尝试了n次，而且花了1天发现根本无法支持sourcemap，而且看的教程也没有支持，所以就先不考虑支持sourcemap

  React 仓库常用的是 yarn build 或 yarn build react/index,react-dom/index（不同版本脚本略有差异）。
  执行：

```sh
yarn build
```

报错，没有jdk

```sh
brew install openjdk@17
# 苹果芯片：
export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"
java -version
# intel芯片
export PATH="/usr/local/opt/openjdk@17/bin:$PATH"
java -version
```

执行：yarn build-combined --type=UMD  生成更加全的内容

如果构建很慢/想只构建核心包，试类似（看你仓库 scripts 支持什么）：
yarn build react/index,react-dom/index --type=NODE
或
yarn build react/index,react-dom/index --type=DEV

构建核心包的话其他包会丢失，不行；

3）运行 fixtures
一般 fixtures 在：
cd  fixtures/dom

npm run start
